# Runtime dependencies for the Vercel serverless function (api/index.py).
# Deliberately WITHOUT the ML/Jupyter stack (numpy/pandas/sklearn/nbclient…):
# it does not fit a serverless bundle, and notebook execution is disabled
# there. The full stack lives in ../backend/requirements.txt for local runs.
# psycopg[binary] is included so NEXUSAI_DATABASE_URL can point at hosted
# PostgreSQL for persistent data (the default is ephemeral SQLite in /tmp).
fastapi>=0.111,<1.0
sqlalchemy>=2.0.30,<3.0
psycopg[binary]>=3.1,<4.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
email-validator>=2.1,<3.0
PyJWT>=2.8,<3.0
bcrypt>=4.1,<5.0
httpx>=0.27,<1.0
